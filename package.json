{
  "name": "responsive-web-design",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/hrdtbs/responsive-web-design.git",
  "author": "hrdtbs",
  "license": "MIT",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy":
      "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ && git commit -m \"Deploy Next.js to gh-pages\" && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "isomorphic-fetch": "^2.2.1",
    "next": "latest",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-fast-highlight": "^2.2.0",
    "react-markdown": "^3.3.0"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.0"
  }
}
